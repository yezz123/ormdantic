# Enterprise Dialects

Default Python wheels include the Rust drivers for PostgreSQL, MySQL, MariaDB, SQL Server, and Oracle. Use `runtime_capabilities()` to inspect the installed extension.

```python
--8<-- "examples/enterprise_dialects.py"
```
