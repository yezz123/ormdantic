# Migrations And Reflection

The migration facade executes SQL plans and stores applied revisions through the Rust runtime. The inspector exposes Rust-backed runtime metadata for supported dialects.

```python
--8<-- "examples/migrations_reflection.py"
```
