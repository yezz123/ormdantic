# SQLAlchemy Parity

This matrix tracks Ormdantic vNext against SQLAlchemy-style capabilities.

| Area                                  | Status  | Notes                                                                                                                                                    |
| ------------------------------------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Core CRUD                             | Full    | Insert, update, upsert, delete, find one, find many, count compile through Rust.                                                                         |
| Bind parameters                       | Full    | Queries use driver placeholders, ordered bind values, and dialect-specific maximum bind-parameter enforcement before runtime execution.                    |
| SQLite runtime                        | Full    | Native Rust execution with `rusqlite`.                                                                                                                   |
| PostgreSQL runtime                    | Full    | Native Rust support with gated CI integration.                                                                                                           |
| MySQL runtime                         | Full    | Native Rust support with gated CI integration.                                                                                                           |
| MariaDB runtime                       | Full    | Native Rust support via MySQL protocol with gated CI integration.                                                                                        |
| SQL Server runtime                    | Full    | Native Rust support through the pure Rust TDS driver compiled into default Python wheels.                                                                |
| Oracle runtime                        | Full    | Native Rust support through the pure Rust TNS driver compiled into default Python wheels.                                                                |
| Query expressions                     | Full    | Comparisons, boolean groups, CTEs, windows, subqueries, relationship predicates, relation count ordering, projections, aggregates, scalar expressions, typed updates, expression hooks, and SQLite Decimal-aware typed expression filters/orderings are available. |
| DDL                                   | Partial | Tables, keys, enum value checks, Pydantic string pattern checks for SQLite/PostgreSQL/MySQL/MariaDB/Oracle with SQL Server unsupported-feature errors, Pydantic numeric `multiple_of` checks with exact SQLite decimal enforcement, Pydantic Decimal precision/scale inference from `max_digits` and `decimal_places` with server reflection preservation, PostgreSQL native enum type snapshots/DDL/reflection including enum type comments and cross-schema enum types referenced by reflected tables, string Enum model snapshots, PostgreSQL enum column DDL/reflection including schema-qualified type names, custom enum type naming/schema controls, runtime enum create/drop lifecycle, registered namespace/schema snapshots/DDL/planning/runtime lifecycle for PostgreSQL/MySQL/MariaDB/SQL Server, PostgreSQL/SQL Server namespace comments with reflection/runtime creation, externally referenced enum/sequence schemas, schema-owned table snapshots/DDL/runtime SQL/autogenerate scoping, schema-aware migration diff identity, and schema-qualified foreign-key DDL references for explicit and relationship-derived FKs with include/exclude filtering for target tables, views, inferred enum types, referenced sequences, and referenced namespaces, registered sequence snapshots/DDL/reflection/runtime lifecycle including PostgreSQL/MariaDB/SQL Server sequence data types, Oracle sequence ordering, PostgreSQL/SQL Server sequence comments, sequence no-bound authoring flags with dialect default-bound no-churn diff normalization, MariaDB/SQL Server/Oracle option reflection, and PostgreSQL cross-schema sequences referenced by reflected table defaults, registered regular view snapshots/DDL/reflection/runtime lifecycle across PostgreSQL/MySQL/MariaDB/SQL Server/Oracle plus PostgreSQL/SQL Server/Oracle regular-view comments, PostgreSQL/Oracle materialized-view snapshots/planning/reflection/runtime lifecycle plus PostgreSQL/Oracle materialized-view comments, table comments and column comments for PostgreSQL/MySQL/MariaDB/SQL Server/Oracle, PostgreSQL/Oracle table tablespaces, Oracle table compression, MySQL/MariaDB table tablespace DDL/planning/reflection, SQL Server table filegroup create-time DDL/reflection, MySQL/MariaDB table engine/charset/collation/row-format/KEY_BLOCK_SIZE/PACK_KEYS/CHECKSUM/DELAY_KEY_WRITE/STATS_PERSISTENT/STATS_AUTO_RECALC/STATS_SAMPLE_PAGES/AVG_ROW_LENGTH/MAX_ROWS/MIN_ROWS/INSERT_METHOD/DATA_DIRECTORY/INDEX_DIRECTORY/CONNECTION/UNION/PARTITION BY/PARTITIONS/SUBPARTITION BY/SUBPARTITIONS/AUTO_INCREMENT options, column collation snapshots/DDL/reflection for SQLite/PostgreSQL/MySQL/MariaDB/SQL Server/Oracle, SQLite generated-column and autoincrement reflection, server generated/computed-column reflection for PostgreSQL/MySQL/MariaDB/SQL Server/Oracle, server identity/autoincrement snapshots/DDL/reflection for PostgreSQL/MySQL/MariaDB/SQL Server/Oracle including PostgreSQL/Oracle identity min/max/cycle/cache/no-bound options with dialect default-bound no-churn diff normalization, Oracle identity ordering, and Oracle identity `ON NULL`, PostgreSQL table inheritance snapshots/DDL/reflection/planning, PostgreSQL table storage parameters, table access methods, unlogged table persistence, partition keys, child partition snapshots/DDL/reflection/planning, and child partition attach/detach migration planning, named table checks, generated field CHECK constraints that round-trip back to column metadata during reflection, PostgreSQL `NOT VALID`, MySQL `NOT ENFORCED`, SQL Server `WITH NOCHECK`, and Oracle `ENABLE NOVALIDATE` table checks plus public table-level and metadata-bearing single-column foreign keys, PostgreSQL check `NO INHERIT` clauses, PostgreSQL/SQL Server table-constraint comments, named unique constraints including metadata-bearing single-column uniques, SQLite primary-key/not-null/unique conflict clauses, named foreign-key timing, composite and metadata-bearing single-column table-level foreign-key reflection, and `STRICT`/`WITHOUT ROWID` table options, PostgreSQL unique `NULLS NOT DISTINCT` clauses and unique-constraint `INCLUDE` columns, SQL Server unique-constraint clustered/nonclustered clauses and filegroups, Oracle unique-constraint index tablespaces/compression, composite table-level foreign keys, PostgreSQL/SQLite foreign-key `MATCH` options, PostgreSQL exclusion constraint snapshots/DDL/reflection/planning including operator classes, dialect-gated deferrable timing for PostgreSQL/Oracle unique and foreign-key constraints, SQLite foreign keys, and PostgreSQL exclusion constraints, foreign-key actions, dialect-aware autoincrement/identity columns, backend-specific create/drop index syntax, simple indexes, public advanced index metadata, PostgreSQL/MySQL/MariaDB/SQL Server index comments, MySQL/MariaDB index prefixes, prefix lengths, and `USING` methods, MySQL index visibility, PostgreSQL index tablespaces, unique-index `NULLS NOT DISTINCT`, Oracle index tablespaces, bitmap indexes, and compression, SQL Server index filegroups and clustered indexes with nonclustered primary-key rendering when needed, public column defaults/computed metadata, PostgreSQL/SQLite partial indexes, SQL Server filtered indexes, PostgreSQL/SQLite expression indexes, PostgreSQL/SQL Server INCLUDE columns, PostgreSQL index methods, operator classes, and storage parameters with default `btree` no-churn diff normalization, schema-diff SQL, metadata-preserving generated SQLite table rebuilds, and SQLite Decimal CHECK/filter/order semantics across legacy and typed expression paths are covered with explicit unsupported-feature errors on dialects that cannot render them; other advanced database-native objects remain future work. |
| Relationships                         | Full    | Rust-owned joined/depth loading, dialect-limit-aware chunked select-in loading, explicit lazy loading, `noload`, loader filters, loader ordering, and SQLite plus external database-matrix stress suites are available. |
| Transactions                          | Full    | Begin/commit/rollback, savepoints, and backend-specific transaction option rendering for isolation levels, read-only mode, and PostgreSQL deferrable flags flow through the native runtime. |
| Sessions/unit of work                 | Full    | Identity map, cascaded add/delete for loaded graphs, dependency-ordered mixed graph flushes, detached merge/expire semantics, dirty tracking, refresh, commit, and rollback are available. |
| Events                                | Full    | CRUD, transaction, flush, handler removal, and handler clearing lifecycle events are available.                                                          |
| Migrations                            | Full    | Artifact V2 checksums, durable history, explicit rollback, status/history/current/repair/check CLI commands, live autogenerate, and metadata-preserving SQLite rebuild plans. |
| Reflection                            | Partial | Runtime inspection and live autogenerate cover supported database backends for core table metadata; generated field CHECK constraints normalize back to column snapshots, server numeric precision/scale and column collation are preserved, generated/computed columns reflect for PostgreSQL/MySQL/MariaDB/SQL Server/Oracle, identity/autoincrement metadata reflects for PostgreSQL/MySQL/MariaDB/SQL Server/Oracle including PostgreSQL/Oracle identity min/max/cycle/cache options, Oracle identity ordering, and Oracle identity `ON NULL`, SQLite generated columns, autoincrement flags, unique/index columns, partial/expression index predicates, column-level primary-key/not-null/unique conflict clauses, named table-constraint metadata, named unique conflict clauses, named foreign-key timing, composite table-level foreign keys, `STRICT`/`WITHOUT ROWID` table options, and views, table and column comments for PostgreSQL/MySQL/MariaDB/SQL Server/Oracle, explicit PostgreSQL/Oracle/MySQL/MariaDB table tablespaces, Oracle table compression, SQL Server non-default table filegroups, MySQL/MariaDB table engine/charset/collation/row-format/KEY_BLOCK_SIZE/PACK_KEYS/CHECKSUM/DELAY_KEY_WRITE/STATS_PERSISTENT/STATS_AUTO_RECALC/STATS_SAMPLE_PAGES/AVG_ROW_LENGTH/MAX_ROWS/MIN_ROWS/INSERT_METHOD/DATA_DIRECTORY/INDEX_DIRECTORY/CONNECTION/UNION/PARTITION BY/PARTITIONS/SUBPARTITION BY/SUBPARTITIONS/AUTO_INCREMENT options, namespace/schema objects and schema-scoped table snapshots for PostgreSQL/MySQL/MariaDB/SQL Server including PostgreSQL/SQL Server namespace comments, external enum/sequence schemas, and cross-schema foreign-key references, PostgreSQL table inheritance, table storage parameters, table access methods, unlogged table persistence, partition keys, and child partitions, PostgreSQL enum types including enum type comments, cross-schema enum types referenced by reflected tables, schema-qualified enum column kinds, PostgreSQL/MariaDB/SQL Server sequence data type reflection, Oracle sequence ordering reflection, MariaDB/SQL Server/Oracle sequence option reflection, PostgreSQL/SQL Server sequence comments, PostgreSQL cross-schema sequences referenced by reflected table defaults, and PostgreSQL scoped sequence reflection limited to table-referenced sequences, regular view catalog reflection for PostgreSQL/MySQL/MariaDB/SQL Server/Oracle with include/exclude filters plus PostgreSQL/SQL Server/Oracle regular-view comments, PostgreSQL/Oracle materialized-view reflection plus PostgreSQL/Oracle materialized-view comments, exclusion constraints including PostgreSQL operator classes, composite foreign keys, PostgreSQL `NOT VALID`, MySQL `NOT ENFORCED`, SQL Server `is_not_trusted`, and Oracle `NOT VALIDATED` table checks and table-level plus metadata-bearing single-column foreign keys, PostgreSQL check `NO INHERIT` clauses, PostgreSQL/SQL Server table-constraint comments, PostgreSQL unique `NULLS NOT DISTINCT` clauses and unique-constraint `INCLUDE` columns including metadata-bearing single-column unique constraints, SQL Server unique-constraint clustered flags and filegroups, Oracle deferrable unique constraints and unique-constraint index tablespaces/compression, PostgreSQL/SQLite foreign-key `MATCH` options, PostgreSQL index predicates/expressions/includes/operator classes/storage parameters/tablespaces/unique-index `NULLS NOT DISTINCT` plus PostgreSQL/MySQL/MariaDB/SQL Server index comments, MySQL/MariaDB index prefixes, prefix lengths, and non-default `USING` methods, MySQL invisible-index metadata, SQL Server index filegroups and clustered flags, Oracle index tablespaces, bitmap flags, and compression, and default `btree` method normalization, SQL Server filtered/includes/clustered-index metadata, views, and composite foreign keys, MySQL/MariaDB views and composite foreign keys, Oracle views/materialized views and composite foreign keys, named multi-column unique constraints, named table checks including MySQL/MariaDB/SQL Server catalog reflection, foreign-key names/actions, and autogenerate preservation for reflected enum, namespace, sequence, and regular/materialized view objects are covered, while broader advanced objects remain planned. |
| Association proxy / hybrid attributes | Full    | Python descriptors and explicit expression hooks lower association proxies and hybrid attributes into the query-expression facade.                       |

Ormdantic intentionally differs from SQLAlchemy where async safety requires explicit behavior, such as explicit relationship loading instead of hidden synchronous lazy loads.

## Remaining Work For A Clean, Fast ORM

Clean means the public API stays small, explicit, typed, and predictable. Fast means hot paths stay Rust-owned, benchmarked, and free of hidden round trips. The feature surface is close; the remaining work is mostly about no-churn schema fidelity, backend-native edge cases, and durable cross-dialect release gates.

| Track                     | What is left                                                                                                                                                                                                                 | Done when                                                                                                         |
| ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| DDL and schema objects    | Keep the SQLAlchemy-style identity/sequence no-bound flags (`nominvalue` and `nomaxvalue`) and the represented-schema no-drift gate in the external release matrix. Public API fields, snapshot serialization, dialect DDL, valid SQL Server/Oracle create/drop table rendering, Oracle default-before-nullability rendering, Oracle identity option parsing, existing-object no-op diff handling, dialect default-bound normalization, MySQL/MariaDB unmanaged `AUTO_INCREMENT` counter normalization, bounded string DDL, backend-specific alter-column syntax, relationship-derived FK type/length inheritance, core metadata reflection, and live PostgreSQL/MySQL/MariaDB/SQL Server/Oracle no-drift coverage are in place. The remaining work is triaging rarer backend-native physical storage, constraint, and object clauses that are still outside the represented table/index/constraint/enum/sequence/view surface. Keep those clauses out of the portable API unless snapshots, DDL, reflection, diffs, and explicit unsupported-feature errors can all be provided. | Supported schema features round-trip through snapshots, dialect DDL, reflection, runtime SQL, and migration planning without manual-change churn. |
| Reflection and migrations | The external migration module now passes all 46 migration execution, live autogenerate, core metadata reflection, represented metadata no-drift, PostgreSQL/MySQL/MariaDB/SQL Server index-comment no-drift, PostgreSQL/SQL Server constraint-comment no-drift, PostgreSQL enum-type comment no-drift, PostgreSQL/SQL Server namespace-comment no-drift, sequence no-bound, PostgreSQL/SQL Server sequence-comment no-drift, identity no-bound, MySQL/MariaDB autoincrement-counter, all-backend regular-view definition no-drift, and PostgreSQL/Oracle materialized-view definition no-drift tests across PostgreSQL, MySQL, MariaDB, SQL Server, and Oracle. The newest no-drift gates cover table/column comments, server defaults, bounded string checks, numeric checks, named unique constraints, indexes, relationship-derived foreign keys, FK actions, PostgreSQL/MySQL/MariaDB/SQL Server index comments, PostgreSQL/SQL Server constraint comments, PostgreSQL enum type comments, PostgreSQL/SQL Server namespace comments, PostgreSQL/SQL Server sequence comments, MySQL/MariaDB `AUTO_INCREMENT` counters advanced by real inserts, PostgreSQL catalog-formatted simple registered regular/materialized view definitions, MySQL/MariaDB catalog-rewritten simple registered view definitions, SQL Server `CREATE VIEW` catalog wrappers and batch execution, Oracle regular-view `TEXT_VC` reads, Oracle materialized-view `DBMS_METADATA` definition extraction and default snapshot-comment normalization, backend default collation/tablespace normalization, paginated Oracle history metadata/version reads, and no-op repair handling for clean migration history rows. Finish stable autogenerate behavior for broader represented objects that still need backend-specific catalog normalization, then define clear downgrade/ignore behavior for native objects that remain intentionally unmodeled. | A live database inspected after an Ormdantic migration produces an equivalent target snapshot or an intentional unsupported/ignored-object result. |
| Dialect hardening         | Keep SQL Server and Oracle in the required external database service job, and keep expanding backend-specific DDL/reflection, numeric decoding, Oracle/SQL Server driver-read edge cases, and structured execution-error suites for every advertised native backend.                                            | Runtime, migrations, reflection, relationship stress, and driver-edge suites run regularly against SQLite, PostgreSQL, MySQL, MariaDB, SQL Server, and Oracle. |
| Value fidelity            | Keep exact numeric release gates across every supported backend, including Decimal values, high-range unsigned integers, and SQLite typed-expression Decimal filtering/ordering. Add backend-specific edge cases as dialects expose them.                                | Driver decoding, Python bridge conversion, and strict model hydration preserve values without float coercion or signed-width loss. |

Performance guardrails are now part of the release gate rather than a remaining feature gap. The CodSpeed workflow runs the Python ORM benchmark suite and Rust Criterion/CodSpeed benches for query compilation, hydration, select-in merging, relationship loading, migrations/reflection, dialect rendering, Python bridge conversion, and runtime driver execution.

## Completion Gates

The remaining work should be considered done only when these gates pass:

- Relationship loading: large select-in batches are capped by backend bind-parameter limits, and deeply nested mixed loader graphs pass the shared stress suite across SQLite, PostgreSQL, MySQL, MariaDB, SQL Server, and Oracle with stable identity merging and no hidden round trips; the external database matrix runner includes this stress suite.
- DDL/reflection/migrations: public column defaults/computed/identity metadata, including SQLite generated-column/autoincrement reflection, server generated/computed-column plus identity/autoincrement reflection, PostgreSQL/Oracle identity min/max/cycle/cache options, Oracle identity ordering, and Oracle identity `ON NULL`, column collation, enum value checks, field comparison/length/pattern/multiple-of checks with generated-check reflection normalization, Pydantic Decimal precision/scale inference with live server reflection preservation, PostgreSQL native enum type snapshots/DDL/reflection including enum type comments and cross-schema enum types referenced by reflected tables, string Enum model snapshots, PostgreSQL enum column DDL/reflection including schema-qualified type names, custom enum type naming/schema controls, runtime enum create/drop lifecycle, registered namespace/schema snapshots/DDL/reflection/planning/runtime lifecycle including PostgreSQL/SQL Server namespace comments and external enum/sequence schemas, schema-owned table snapshots/dialect DDL/runtime SQL/scoped autogenerate, schema-aware migration diff identity, schema-qualified foreign-key DDL references for explicit and relationship-derived FKs, and reflected cross-schema foreign-key references with include/exclude filtering on reflected and target tables/views plus target inferred enum types, target referenced sequences/namespaces, live PostgreSQL sequence reflection limited to scoped table references, PostgreSQL/MariaDB/SQL Server sequence data type reflection, Oracle sequence ordering reflection, MariaDB/SQL Server/Oracle sequence option reflection, and registered sequence snapshots/DDL/reflection/runtime lifecycle including PostgreSQL/MariaDB/SQL Server sequence data types, Oracle sequence ordering, PostgreSQL/SQL Server sequence comments, no-bound flags, and cross-schema sequences referenced by reflected table defaults, registered regular view snapshots/DDL/reflection/runtime lifecycle across PostgreSQL/MySQL/MariaDB/SQL Server/Oracle including PostgreSQL/SQL Server/Oracle regular-view comments and PostgreSQL/MySQL/MariaDB simple-view rewrite no-drift normalization plus SQL Server `CREATE VIEW` wrapper normalization and Oracle `TEXT_VC` definition reads, autogenerate preservation for reflected enum, namespace, sequence, and regular/materialized view objects when the target snapshot has none, PostgreSQL/Oracle materialized-view snapshots/planning/reflection/runtime lifecycle including PostgreSQL/Oracle materialized-view comments, PostgreSQL materialized-view definition no-drift normalization, Oracle materialized-view definition extraction/default-comment normalization/table-reflection filtering, table comments and column comments on PostgreSQL/MySQL/MariaDB/SQL Server/Oracle, PostgreSQL/Oracle table tablespaces, Oracle table compression, MySQL/MariaDB table tablespace DDL/planning/reflection, SQL Server table filegroup create/reflection, and MySQL/MariaDB table engine/charset/collation/row-format/KEY_BLOCK_SIZE/PACK_KEYS/CHECKSUM/DELAY_KEY_WRITE/STATS_PERSISTENT/STATS_AUTO_RECALC/STATS_SAMPLE_PAGES/AVG_ROW_LENGTH/MAX_ROWS/MIN_ROWS/INSERT_METHOD/DATA_DIRECTORY/INDEX_DIRECTORY/CONNECTION/UNION/PARTITION BY/PARTITIONS/SUBPARTITION BY/SUBPARTITIONS/AUTO_INCREMENT options with unmanaged runtime counters normalized out of no-drift comparisons, PostgreSQL table inheritance snapshots/DDL/reflection/planning, PostgreSQL table storage parameters, table access methods, unlogged table persistence, partition keys, child partition snapshots/DDL/reflection/planning, and child partition attach/detach migration planning, named table checks including MySQL/MariaDB/SQL Server catalog reflection, PostgreSQL `NOT VALID`, MySQL `NOT ENFORCED`, SQL Server `WITH NOCHECK`, and Oracle `ENABLE NOVALIDATE` table checks and public table-level plus metadata-bearing single-column foreign keys with live reflection preservation, PostgreSQL check `NO INHERIT` clauses, PostgreSQL/SQL Server table-constraint comments, named unique constraints including Oracle deferrable timing and unique-constraint index tablespace/compression reflection plus metadata-bearing single-column unique preservation, SQLite primary-key/not-null/unique conflict clauses including live column-level reflection, named foreign-key timing, composite and metadata-bearing single-column table-level foreign-key reflection, and `STRICT`/`WITHOUT ROWID` table options, PostgreSQL unique `NULLS NOT DISTINCT` clauses and unique-constraint `INCLUDE` columns, SQL Server unique-constraint clustered/nonclustered clauses and filegroups, composite table-level foreign keys, PostgreSQL/SQLite foreign-key `MATCH` options, PostgreSQL exclusion constraint snapshots/DDL/reflection/planning including operator classes, dialect-gated deferrable unique/foreign-key/exclusion timing, foreign-key actions, and public advanced indexes including dialect-gated partial/expression/include metadata, live SQLite partial/expression index reflection, SQL Server filtered/include index metadata, PostgreSQL index operator classes/storage parameters/tablespaces/unique-index `NULLS NOT DISTINCT`, Oracle index tablespaces/bitmap/compression, PostgreSQL/MySQL/MariaDB/SQL Server index comments, MySQL/MariaDB index prefixes, prefix lengths, and non-default `USING` methods, MySQL invisible-index metadata, SQL Server index filegroups and clustered indexes, and default PostgreSQL `btree` method plus identity/sequence no-bound default-value no-churn normalization keep round-tripping through dialect-aware DDL rendering and migration snapshots; the full 46-test external migration module now passes for migration smoke, live autogenerate, core metadata reflection, represented metadata no-drift, PostgreSQL/MySQL/MariaDB/SQL Server index-comment drift, PostgreSQL/SQL Server constraint-comment drift, PostgreSQL enum-type comment drift, PostgreSQL/SQL Server namespace-comment drift, sequence no-bound drift, PostgreSQL/SQL Server sequence-comment drift, identity no-bound drift, MySQL/MariaDB autoincrement-counter drift, regular-view definition drift across PostgreSQL, MySQL, MariaDB, SQL Server, and Oracle, and materialized-view definition drift across PostgreSQL and Oracle, while rarer backend-specific constraint clauses, physical-storage clauses, and broader advanced-object reflection still need autogenerate coverage on every applicable backend.
- Identity/sequence no-bound parity: `nominvalue` and `nomaxvalue` stay covered by `test_external_sequence_no_bound_defaults_do_not_autogenerate_drift` for PostgreSQL, MariaDB, SQL Server, and Oracle plus `test_external_identity_no_bound_defaults_do_not_autogenerate_drift` for PostgreSQL and Oracle, proving live reflection/default-bound normalization does not create false drift after round-trip inspection.
- Dialects: backend-specific transaction option rendering, compiler-enforced bind-parameter limits, loader-side select-in batch capping, and structured execution-error classification are covered in tests, and the external database service job runs migration smoke, live autogenerate, relationship stress, engine driver, SQL Server, and Oracle execution suites; DDL/reflection, numeric decoding, and rarer backend-specific error codes should keep expanding in regular CI for every advertised native backend where infrastructure is available.
- Value fidelity: exact decimal values and large unsigned integers survive driver decoding, Python bridge conversion, and model hydration without lossy float or signed-width coercion on every supported backend; PostgreSQL, MySQL, MariaDB, SQL Server, and Oracle decimal decoding are covered by driver-edge tests and external Python bridge/model hydration tests, MySQL/MariaDB native unsigned decoding is covered by driver-edge tests and external strict-`int` hydration tests, SQLite declared `NUMERIC`/`DECIMAL` columns recover `Decimal` objects, Ormdantic-created SQLite decimal columns use `DECIMAL_TEXT` text affinity so high-precision decimal storage remains exact, SQLite declared `INTEGER` columns recover out-of-signed-range unsigned values from exact overflow storage and hydrate strict `int` models, and SQLite decimal filters, typed expression filters/orderings, and generated checks use deterministic decimal comparator/sort-key functions instead of lexicographic text comparison.
- Performance: `.github/workflows/codspeed.yml` keeps Python ORM benchmarks (`uv run pytest tests/benchmarks --codspeed`) and Rust CodSpeed benches (`cargo codspeed build` plus `cargo codspeed run`) in release gating for query compilation, hydration, select-in merging, relationship loading, migrations/reflection, dialect rendering, Python bridge conversion, and runtime driver execution.

Non-goals remain important: Ormdantic should not become a full SQLAlchemy clone, hide synchronous lazy loads behind attribute access, or move query compilation and hydration hot paths back into Python.
